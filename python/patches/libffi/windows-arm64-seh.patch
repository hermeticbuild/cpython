--- a/src/aarch64/sysv.S
+++ b/src/aarch64/sysv.S
@@ -92 +92,10 @@
-CNAME(ffi_call_SYSV):
+#if defined(_WIN32)
+	.seh_proc CNAME(ffi_call_SYSV_fake)
+CNAME(ffi_call_SYSV_fake):
+	stp	x29, x30, [sp, #-32]!
+	.seh_save_fplr_x 32
+	mov	x29, sp
+	.seh_set_fp
+	.seh_endprologue
+#endif
+CNAME(ffi_call_SYSV):
@@ -162 +171,6 @@
-	.align	4
+#if defined(_WIN32)
+	/* LLVM's ARM64 SEH emitter cannot evaluate .align inside a function. */
+	nop
+#else
+	.align	4
+#endif
@@ -297 +311,4 @@
-	cfi_endproc
+	cfi_endproc
+#if defined(_WIN32)
+	.seh_endproc
+#endif
